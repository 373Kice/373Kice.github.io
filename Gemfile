source "https://rubygems.org"

# Jekyll and common plugins used by this site
# 移除版本限制以获取最新版本
gem "jekyll"
gem "jekyll-paginate"
gem "kramdown"
gem "rouge"
# sassc已弃用，改用sass-embedded
# gem "sassc", github: "sass/sassc-ruby" if RUBY_PLATFORM =~ /mswin|mingw/

# For Ruby >= 3.0, webrick is not bundled
gem "webrick"

# Windows-specific gem to avoid polling for changes
gem "wdm" if Gem.win_platform?
